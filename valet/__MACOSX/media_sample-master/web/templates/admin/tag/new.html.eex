<h2><%= gettext "New tag" %></h2>

<%= render "form.html", changeset: @changeset,
                        tag: nil,
                        action: admin_tag_path(@conn, :create, @conn.assigns.locale) %>

<%= link gettext("Back"), to: admin_tag_path(@conn, :index, @conn.assigns.locale) %>
